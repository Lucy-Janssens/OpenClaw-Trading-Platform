# 2026-03-14
- Started working on OpenClaw Binance MVP.
- Investigated the current codebase (placeholder FastAPI + vanilla JS UI).
- Drafted an implementation plan using `ccxt` for Binance integration, `SQLAlchemy` + `PostgreSQL` for state/guardrails, and a background asyncio loop for the trading agent.
- Asked the human for Binance credentials preference (Testnet vs Real keys) and if there is a specific simple strategy they want the agent to start with.
